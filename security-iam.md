# Security and IAM Configuration

- Used IAM roles for EC2 instances instead of hard-coded credentials.
- Applied least-privilege access while creating IAM policies.
- Configured security groups to control inbound and outbound traffic.
- Used public and private subnets to isolate resources.
- Followed basic AWS security best practices during lab exercises.
